# Environment files (NEVER commit secrets)
.env
backend/.env
frontend/.env

# Dependencies
node_modules/

# Build outputs
dist/
coverage/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Scratch de execucao local
backend/_*.txt
frontend/tsconfig.tsbuildinfo

# `.claude/` é versionado de propósito: o desafio pede para versionar os
# artefatos de contexto usados com IA. O que fica de fora é só o que é da
# máquina ou da sessão.
.claude/settings.local.json
.claude/scheduled_tasks.lock
