{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "permissions": {
    "allow": [
      "Bash(npm test:*)",
      "Bash(npm run build:*)",
      "Bash(npx jest:*)",
      "Bash(npx tsc:*)",
      "Bash(docker compose ps:*)",
      "Bash(docker compose logs:*)",
      "Bash(git status:*)",
      "Bash(git diff:*)",
      "Bash(git log:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); case \"$f\" in *backend/src/*) docker compose logs api --tail 25 2>&1 | grep -iE 'error|exception|failed' | tail -5 || true;; esac 2>/dev/null || true",
            "statusMessage": "Conferindo o log da API depois de mexer no backend..."
          }
        ]
      }
    ]
  }
}
